#!/bin/bash

echo "${PROJECT_NAME} RUNNER..."
echo "... LD_LIBRARY_PATH=$ENV{LD_LIBRARY_PATH} ${RUNTIME_OUTPUT_DIR}/${PROJECT_NAME}"

LD_LIBRARY_PATH=$ENV{LD_LIBRARY_PATH} ${RUNTIME_OUTPUT_DIR}/${PROJECT_NAME}


exit -1